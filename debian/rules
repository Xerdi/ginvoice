#!/usr/bin/make -f
export DH_VIRTUALENV_ARGUMENTS := --system-site-packages
export PYBUILD_NAME=ginvoice
%:
	dh $@ --with python-virtualenv -S
override_dh_install:
	scripts/packres.sh
	dh_install
override_dh_auto_test:
	:
